Actuarial study to model premium adjustment for Chile's mandatory vehicle insurance (SOAP), paired with a natural language agent that lets non-technical stakeholders query the model and receive semantic, explained output rather than raw figures. The statistical layer is fully separated from the language layer: the model computes, the agent interprets and explains. Built for a government client where every recommendation had to be defensible and traceable to its underlying method.
